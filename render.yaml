services:
  - type: web
    name: crimax-sports-api
    runtime: python
    plan: free
    # Live rooms are process-local (in-memory WebSocket manager).
    # Keep a single instance until Redis pub/sub (or sticky sessions) lands.
    numInstances: 1
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /ready
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: crimax-sports-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 60
      - key: REFRESH_TOKEN_EXPIRE_DAYS
        value: 7
      - key: ALLOWED_ORIGINS
        value: https://sports.crimax.ng,http://localhost:3000

databases:
  - name: crimax-sports-db
    plan: free
    databaseName: crimax_sports
